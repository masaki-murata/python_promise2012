入出力は各辺32以上で、2**nの形とする(ここを一般化するにはコードをちょっといじる)
訓練データは元画像、セグメンテーション画像共に0-1に規格化し, 
(batch_size, width, height, depth, channels)の順に並べたnumpy配列にしておく

seunet_main.pyを、次の順でコマンドライン引数を与えてpython3で実行:
1. 元画像配列へのパス
2. セグメンテーション画像配列へのパス
3. 重みを保存するディレクトリのパス
4. バッチサイズ
5. エポック数
6. 使うGPUの枚数(4?)
